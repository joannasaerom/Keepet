<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epicodus.pettracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"></activity>
        <activity android:name=".ui.WeightActivity" />
        <activity android:name=".ui.MedicationActivity" />
        <activity android:name=".ui.MedicationDetailActivity" />
        <activity android:name=".ui.VetActivity" />
        <activity android:name=".ui.NewMedicationActivity" />
        <activity android:name=".ui.NewPetActivity" />
        <activity android:name=".ui.PetListActivity" />
        <activity android:name=".ui.VetDetailActivity" />
        <activity android:name=".ui.CreateAccountActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>